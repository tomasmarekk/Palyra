# iOS Scaffold

This is the iOS node application scaffold for future milestones.

## Lint baseline

- `SwiftLint` configuration lives in `apps/ios/.swiftlint.yml`.
- Recommended local command:
  - `swiftlint lint --config apps/ios/.swiftlint.yml`

No app runtime code is shipped in M02.
