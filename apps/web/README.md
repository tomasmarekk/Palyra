# Web Scaffold

This directory is the scaffold for the future web console.

## Lint baseline

- ESLint flat config: `apps/web/eslint.config.mjs`
- TypeScript config: `apps/web/tsconfig.json`
- Optional local lint command:
  - `bash apps/web/scripts/lint.sh`

No production web UI is shipped in M02.
