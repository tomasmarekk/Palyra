[graph]
targets = []
all-features = true

[advisories]
version = 2
ignore = [
  # rustls-pemfile is a transitive dependency (via reqwest/rustls stack) with no drop-in
  # maintained replacement in current upstream graph; tracked until dependency chain migrates.
  "RUSTSEC-2025-0134",
]

[licenses]
version = 2
allow = [
  "Apache-2.0",
  "Apache-2.0 WITH LLVM-exception",
  "MIT",
  "FSL-1.1-ALv2",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "ISC",
  "Unicode-3.0",
  "Zlib",
]
exceptions = [
  # headless_chrome currently uses auto_generate_cdp only as a build dependency to
  # generate CDP bindings during compilation; it is not linked into runtime binaries.
  { crate = "auto_generate_cdp", allow = ["GPL-3.0-or-later"] },
  # Transitive through auto_generate_cdp -> ureq; scoped to this crate.
  { crate = "webpki-roots", allow = ["CDLA-Permissive-2.0"] },
]

[[licenses.clarify]]
crate = "palyra-a2ui"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-auth"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-browserd"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-cli"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-common"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-connectors"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-daemon"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-identity"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-plugins-runtime"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-plugins-sdk"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-policy"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-sandbox"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-skills"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-transport-quic"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]
[[licenses.clarify]]
crate = "palyra-vault"
expression = "FSL-1.1-ALv2"
license-files = [{ path = "../../LICENSE", hash = 0x18b96a4b }]

[bans]
multiple-versions = "warn"
wildcards = "deny"
highlight = "all"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
