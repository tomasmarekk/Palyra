schema_version: 1
id: provider.compat.invalid_json_arguments
title: Provider compatibility invalid JSON arguments
area: provider
mode:
  provider: mock
  deterministic: true
requires:
  model: text
  capabilities:
    - qa_lab
    - provider_compat
  tools:
    - palyra.fs.read_file
  fixtures:
    - qa/fixtures/provider_compat/p0_provider_compat_pack.yaml
steps:
  - id: prompt
    action: user_prompt
    prompt: "Classify the invalid_json_arguments provider compatibility fixture."
expect:
  terminal_state: failed
  final_answer:
    contains:
      - "invalid_json_arguments"
      - "malformed_response"
  events:
    - event_type: provider.compat.fixture.loaded
      min_count: 1
    - event_type: provider.recovery.decision
      min_count: 1
  tool_calls: []
forbidden:
  tool_calls:
    - palyra.fs.read_file
  events:
    - tool.call.started
  artifacts: []
  claims: []
artifacts:
  - path: qa/reports/provider/invalid_json_arguments.json
    kind: report
    required: true
maturity:
  labels:
    - p0
    - nightly
    - provider_compat
timeout:
  run_ms: 30000
  step_ms: 10000
